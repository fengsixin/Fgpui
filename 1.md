# Fgpui 单机标准文档生成工具开发计划

## 1. 总体目标与产品边界

### 总体目标

构建一个 Windows 优先的单机桌面应用，面向普通业务用户：

```text
选择文档模板 → 填写表单或导入数据 → 生成标准 PDF → 预览和导出
```

首批支持：

- 技术方案
- 测试报告

应用采用受控模板，模板由开发者维护；普通用户不直接编写 Typst。

### 固定技术方案

- 桌面框架：Tauri 2
- 前端：Vue 3 + TypeScript + Vite
- UI：Element Plus
- 状态管理：Pinia
- PDF 预览：PDF.js
- 核心逻辑：Rust
- 本地数据库：SQLite + rusqlite
- JSON 处理：serde / serde_json
- Excel 导入：calamine
- 数据校验：JSON Schema
- 排版编译：Typst CLI sidecar
- 当前 Typst 基线：`C:\code\Fgpui\typst.exe`，版本为 `0.15.1`
- 文件存储：本机文件系统
- 打包：Tauri Windows 安装包
- 不包含：账号、云同步、服务器、多人协作、消息队列和微服务

---

## 2. 分阶段开发计划

### 阶段 0：工程初始化与 Typst 可行性验证

#### 目标

确认桌面应用、Rust、Vue 和 Typst 编译链路可以稳定工作。

#### 主要工作

- 初始化 Tauri 2 + Vue 3 + TypeScript 项目。
- 初始化 Git 仓库和基础目录结构。
- 将 Typst 0.15.1 作为 sidecar 资源纳入应用。
- 实现 Rust 调用 `typst.exe` 的最小封装。
- 编译一个固定的 `hello.typ` 并生成 PDF。
- 建立统一错误结构和日志结构。

#### 阶段目标状态

应用可以启动，并且点击测试按钮后能够：

```text
调用内置 Typst
编译测试模板
生成 PDF
返回 PDF 文件路径
显示编译失败信息
```

#### 验收标准

- Windows 下能够启动开发版应用。
- Typst 不依赖用户自行安装。
- Typst 缺失、路径错误、编译失败时有明确提示。
- 编译过程不会阻塞前端界面。
- `C:\code\Fgpui\typst.exe` 被作为固定版本基线，不直接依赖系统 Typst。

---

### 阶段 1：本地项目与工作区管理

#### 目标

建立单机应用的核心数据和文件组织方式。

#### 主要工作

- 实现项目创建、打开、保存、删除。
- 实现最近项目列表。
- 实现项目自动保存。
- 使用 SQLite 保存项目索引和生成记录。
- 使用 JSON 保存项目详细数据。
- 建立本地工作目录和备份目录。

#### 固定目录结构

```text
%USERPROFILE%\Documents\FgpuiDocuments\
├── projects\
│   └── {project-id}\
│       ├── project.json
│       ├── data.json
│       ├── assets\
│       ├── output\
│       └── history\
├── templates\
├── fonts\
├── backups\
└── logs\
```

#### 阶段目标状态

用户可以：

```text
新建项目
输入项目名称
选择文档类型
关闭应用后重新打开项目
修改内容后自动保存
查看最近项目
```

#### 验收标准

- 应用异常退出后，已保存项目不会丢失。
- 项目文件可以通过复制目录进行手动备份。
- 删除项目不会误删模板和公共字体。
- 项目数据、输出 PDF 和模板文件分离保存。
- 数据库损坏或项目文件缺失时能显示可理解的恢复提示。

---

### 阶段 2：模板包和数据模型

#### 目标

建立可扩展的文档模板体系，并实现开发者维护模板、用户填写数据的模式。

#### 主要工作

- 实现模板目录扫描。
- 实现模板导入和模板启用。
- 定义模板 manifest。
- 定义 JSON Schema。
- 定义技术方案和测试报告的数据模型。
- 实现示例数据。
- 实现 JSON Schema 到动态表单的映射。
- 实现表单数据保存和加载。

#### 模板目录结构

```text
templates/
└── technical-design/
    ├── manifest.json
    ├── schema.json
    ├── main.typ
    ├── theme.typ
    ├── components/
    ├── assets/
    └── examples/
        └── sample.json
```

#### `manifest.json` 最小接口

```json
{
  "id": "technical-design",
  "name": "技术方案",
  "version": "1.0.0",
  "entry": "main.typ",
  "schema": "schema.json",
  "sample": "examples/sample.json",
  "required_fonts": [
    "CompanySans",
    "CompanyCJK"
  ]
}
```

#### 阶段目标状态

用户可以：

```text
查看技术方案和测试报告模板
选择模板创建项目
看到由 JSON Schema 自动生成的表单
填写标题、作者、章节、表格和图片信息
保存并重新打开表单数据
加载示例数据
```

#### 验收标准

- 表单字段由 Schema 驱动，不把字段结构硬编码在页面中。
- 必填字段、枚举字段、数组字段和嵌套对象可以正常校验。
- 表单数据可以导出为 JSON。
- 同一份 JSON 可以被模板直接用于编译。
- 新增模板时不需要修改核心业务代码，只需要增加模板包。

---

### 阶段 3：Typst 编译与 PDF 预览

#### 目标

打通“表单数据 → Typst → PDF → 预览”的完整核心链路。

#### 主要工作

- 实现 Rust `DocumentCompiler` 抽象。
- 第一版实现 `CliCompiler`。
- 创建隔离的临时编译目录。
- 将项目数据写入 `data.json`。
- 加载模板入口文件。
- 加载模板资源、图片和字体。
- 解析 Typst stdout/stderr。
- 将错误定位到模板文件和行号。
- 使用 PDF.js 实现内嵌预览。
- 实现编译取消和重新编译。

#### 内部编译接口

```rust
pub struct CompileRequest {
    pub project_id: String,
    pub template_path: String,
    pub data_path: String,
    pub output_path: String,
}

pub struct CompileResult {
    pub output_path: String,
    pub warnings: Vec<Diagnostic>,
    pub duration_ms: u64,
}

pub struct Diagnostic {
    pub severity: String,
    pub file: Option<String>,
    pub line: Option<u32>,
    pub column: Option<u32>,
    pub message: String,
}
```

#### 阶段目标状态

用户可以：

```text
填写文档数据
点击生成
看到编译进度
看到 Typst 错误
预览生成的 PDF
重新修改数据并生成
导出 PDF
```

#### 验收标准

- 正常数据可以生成 PDF。
- Typst 编译失败时不会生成伪成功结果。
- 错误信息能显示文件、行号或具体原因。
- 编译期间界面保持可操作。
- PDF 预览无需打开外部浏览器。
- 生成失败不会覆盖上一次成功生成的 PDF。
- 同一项目可以保留最近一次成功输出。

---

### 阶段 4：Excel/JSON 导入与文档内容完善

#### 目标

支持用户通过外部数据快速创建文档，并覆盖首批文档的正式内容。

#### 主要工作

- 支持 JSON 导入。
- 支持 `.xlsx` 和 `.xls` 导入。
- 实现 Excel 列到字段的映射。
- 提供导入预览和错误列表。
- 完成技术方案模板。
- 完成测试报告模板。
- 实现图片导入。
- 实现目录、页眉页脚、编号、表格、修订记录和附录。
- 实现中英文混排和中文字体配置。

#### Excel 导入策略

首版采用“固定字段映射”：

```text
Excel 列名 → Schema 字段
```

不实现复杂的可视化字段映射器。

导入流程：

```text
选择 Excel
    ↓
读取第一个工作表
    ↓
识别列名
    ↓
匹配 Schema 字段
    ↓
显示导入预览
    ↓
报告缺失字段和非法值
    ↓
用户确认
    ↓
写入项目数据
```

#### 阶段目标状态

用户可以使用应用内表单，也可以从 JSON 或 Excel 快速生成一份完整的技术方案或测试报告。

#### 验收标准

- Excel 导入失败时能指出工作表、行号、列名和错误原因。
- 空值、非法日期、非法枚举值不会静默写入。
- 技术方案支持多章节、多级标题、图片和表格。
- 测试报告支持测试范围、测试结果、问题清单和结论。
- 中文字体在目标 Windows 环境中稳定使用。
- 生成的 PDF 适合打印和归档。

---

### 阶段 5：模板版本、可复现生成与质量保障

#### 目标

让生成结果可以追溯、复现和回归测试。

#### 主要工作

- 增加模板版本字段。
- 模板发布后不可修改，只能发布新版本。
- 记录每次生成使用的模板版本。
- 记录 Typst 版本、字体版本和输入数据摘要。
- 增加生成历史。
- 增加模板测试数据。
- 建立 PDF 文本检查和页面数量检查。
- 建立 PDF 页面截图视觉回归。
- 增加编译缓存。
- 增加项目导出和导入备份包。

#### 生成记录最小信息

```json
{
  "generation_id": "gen-001",
  "project_id": "project-001",
  "template_id": "technical-design",
  "template_version": "1.0.0",
  "typst_version": "0.15.1",
  "data_hash": "...",
  "font_hash": "...",
  "output_path": "...",
  "created_at": "..."
}
```

#### 阶段目标状态

用户能够回答：

```text
这份 PDF 是什么时候生成的？
使用了哪个模板版本？
使用了哪份数据？
使用了哪个 Typst 版本？
能否重新生成相同结果？
```

#### 验收标准

- 模板发布后不能被原地修改。
- 历史项目仍可使用原模板版本生成。
- 项目导出包可以在另一台 Windows 机器上恢复。
- 同一模板、同一数据和同一资源能够得到可接受的一致输出。
- 每个正式 PDF 都能追溯到项目、模板和生成记录。
- 模板修改导致的排版变化能够通过视觉回归测试发现。

---

### 阶段 6：Windows 发布与稳定性验收

#### 目标

形成可交付给内部用户安装使用的单机版产品。

#### 主要工作

- 生成 Windows 安装包。
- 打包 Typst sidecar。
- 打包模板和默认字体。
- 增加首次启动向导。
- 增加应用数据目录配置。
- 增加日志导出。
- 增加一键备份。
- 增加版本升级检查提示，但不接入在线自动更新。
- 完成干净机器安装测试。
- 完成长文档和异常数据测试。

#### 阶段目标状态

在一台没有开发环境的 Windows 电脑上，用户可以：

```text
安装应用
启动应用
选择模板
填写或导入数据
生成并预览 PDF
导出 PDF
备份项目
重新打开项目
```

#### 发布验收标准

- 不要求用户安装 Node.js、Rust 或 Typst。
- 不要求用户配置环境变量。
- 安装包可以正常卸载。
- 应用重启后项目和模板仍然可用。
- 缺少字体、图片或模板时能提供修复指导。
- 连续生成 20 份文档不会出现明显内存泄漏或卡死。
- 10 页以上文档能够稳定生成。
- 应用能够保存和导出日志，便于问题定位。

---

## 3. 首版公开接口与核心类型

### Tauri Commands

首版固定提供以下命令：

```text
create_project
open_project
save_project
list_projects
delete_project

list_templates
import_template
get_template_schema
get_template_sample

validate_document_data
import_json_data
import_excel_data

compile_document
cancel_compile
get_compile_status
open_output_file

export_project_backup
import_project_backup
get_app_settings
update_app_settings
```

### 核心前端类型

```ts
type Project = {
  id: string
  name: string
  documentType: string
  templateId: string
  templateVersion: string
  dataPath: string
  createdAt: string
  updatedAt: string
}

type TemplateManifest = {
  id: string
  name: string
  version: string
  entry: string
  schema: string
  sample?: string
  requiredFonts?: string[]
}

type CompileState =
  | "idle"
  | "validating"
  | "compiling"
  | "succeeded"
  | "failed"
  | "cancelled"
```

---

## 4. 测试计划

### 单元测试

覆盖：

- 项目路径生成
- 项目创建和加载
- manifest 解析
- JSON Schema 校验
- Excel 字段映射
- 编译命令参数生成
- Typst 错误解析
- 备份包创建和恢复
- 编译缓存键生成

### 集成测试

覆盖：

- 创建项目并保存
- 加载模板并生成表单
- 表单数据生成 PDF
- JSON 导入生成 PDF
- Excel 导入生成 PDF
- Typst 编译失败后的错误展示
- 删除项目后模板仍然存在
- 项目备份后恢复
- 应用重启后恢复最近项目

### 模板回归测试

每个模板至少包含：

```text
最小数据
完整数据
超长标题
多页表格
空章节
中英文混排
特殊字符
缺失图片
大图片
多级标题
```

### 发布前验收测试

- 干净 Windows 环境安装
- 无 Typst 环境安装
- 无开发工具安装
- 默认字体缺失
- 模板文件缺失
- PDF 输出目录无权限
- 磁盘空间不足
- 编译超时
- 连续批量生成
- 应用异常退出后重新打开

---

## 5. 版本发布节点

### V0.1：技术验证版

目标状态：

```text
能启动桌面应用
能调用 Typst
能生成测试 PDF
```

### V0.2：项目管理版

目标状态：

```text
能创建、保存、打开本地项目
有 SQLite 和本地工作区
```

### V0.3：模板表单版

目标状态：

```text
有技术方案和测试报告模板
能根据 Schema 生成表单
```

### V0.4：可用 MVP

目标状态：

```text
表单/JSON/Excel 输入
Typst 编译
PDF 预览
PDF 导出
错误提示
```

### V0.5：质量增强版

目标状态：

```text
模板版本
生成历史
项目备份
字体管理
回归测试
可复现生成
```

### V1.0：内部正式版

目标状态：

```text
可在干净 Windows 机器安装
普通用户无需开发知识即可生成标准 PDF
技术方案和测试报告达到公司实际使用要求
```

---

## 6. 明确不纳入 V1.0 的功能

以下功能不进入首版开发范围：

- 用户账号和权限系统
- 云端同步
- 多人协作
- 服务端部署
- 在线模板市场
- 在线 Typst 编辑器
- 拖拽式排版编辑器
- DOCX 导出
- HTML 正式输出
- 数字签名和电子印章
- 自动连接 ERP、OA 或项目管理系统
- 多平台同时发布

这些功能只能在 V1.0 稳定后，根据真实使用反馈再规划。

---

## 7. 关键风险与处理策略

### Typst 版本升级风险

处理方式：

- 编译器版本固定。
- 所有生成记录保存 Typst 版本。
- 通过 `DocumentCompiler` 抽象隔离 CLI 和未来的嵌入式编译器。
- 不允许模板直接依赖未验证的 Typst 版本。

### 中文字体差异风险

处理方式：

- 应用自带经过授权的字体包。
- 编译时使用应用指定字体目录。
- 不依赖用户系统字体。
- 模板测试必须覆盖中英文混排。

### 模板排版回归风险

处理方式：

- 模板版本不可变。
- 每个模板维护固定示例数据。
- 每次模板变更执行 PDF 和截图回归测试。

### Excel 数据质量风险

处理方式：

- 首版只支持固定列名映射。
- 导入前显示预览。
- 所有错误定位到工作表、行号和列名。
- 导入不会覆盖原始项目数据，必须用户确认。

### 单机数据丢失风险

处理方式：

- 自动保存。
- 提供项目导出备份包。
- 提供恢复功能。
- 生成 PDF 后不覆盖历史产物。
- 在应用设置中展示数据目录位置。

---

## 8. 默认假设

- 首发平台为 Windows。
- 首发交互为表单驱动，而不是在线写 Typst。
- 首批文档为技术方案和测试报告。
- 首版正式输出只支持 PDF。
- 数据主要来自应用内表单，也支持 JSON 和 Excel 导入。
- 模板由开发者维护，普通用户不直接修改模板。
- 数据只保存在本机，支持手动导出备份。
- 当前代码目录仅有 `C:\code\Fgpui\typst.exe`，因此第一步需要初始化完整桌面应用工程。
- 第一版使用 Typst CLI，未来可以在不改变业务接口的情况下替换成 Rust 嵌入式编译器。
